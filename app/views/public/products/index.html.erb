<div class="container">
	<div class="row">
		<div class="col-xs-2">
			<%= render "genre_select", genres: Genre.all %>
		</div>

		<div class="col-xs-10">
			<div class="row">
			<h2>一覧(全<%= @products.count %>件)</h2>
			</div>
			<% @products.each do |product| %>
			<%= link_to public_product_path(product) do %>
			<div class="list-box" style="display:inline-block;">
			    <%= attachment_image_tag product, :image, :fill, 200, 200, format: 'jpeg' %>
			    <div class="list-title">
			      <p><%= product.name %></p>
			      <p>¥<%= product.price%></p>
			    </div>
			 </div>
			 <% end %>
			<% end %>
		</div>
	</div>
</div>
