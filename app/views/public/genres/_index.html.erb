<table class="table table-bordered">
	<thead>
		<tr>
			<h3>ジャンル一覧</h3>
		</tr>
	</thead>
	<tbody>
		<% genres.each do |genre| %>
		<tr>
			<td>
				<%= link_to genre.name, public_genre_path(genre) %> <br>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>